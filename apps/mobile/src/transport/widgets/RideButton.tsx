/**
 * Rider GPS contribution is disabled — IITJ One no longer collects device location.
 * Live bus positions still come from the backend/socket for viewing only.
 */
export function RideButton() {
  return null;
}
